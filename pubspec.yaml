name: jp_challenge
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^2.0.17


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_gen_runner: ^5.0.0
  build_runner: ^2.4.0

flutter:
  uses-material-design: true

  assets:
    - assets/backgrounds/
    - assets/icons/
    - assets/images/

